<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <script type="text/javascript" 
    src="https://app.sandbox.midtrans.com/snap/snap.js" 
    data-client-key="<%= midtransClientKey %>"></script>
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
    .payment-card {
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
    }
    .status-badge {
      letter-spacing: 0.025em;
    }
  </style>
</head>
<body class="bg-gray-50 dark:bg-gray-900 min-h-screen">
  <div class="container mx-auto px-4 py-8 max-w-6xl">
    <!-- Header with back button -->
    <div class="mb-8 flex items-center">
      <a href="/" class="flex items-center text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M9.707 16.707a1 1 0 01-1.414 0l-6-6a1 1 0 010-1.414l6-6a1 1 0 011.414 1.414L5.414 9H17a1 1 0 110 2H5.414l4.293 4.293a1 1 0 010 1.414z" clip-rule="evenodd" />
        </svg>
        Kembali ke Dashboard
      </a>
    </div>

    <div class="flex flex-col lg:flex-row gap-8">
      <!-- Main payment card -->
      <div class="w-full lg:w-2/3">
        <div class="payment-card bg-white rounded-xl p-8 dark:bg-gray-800">
          <h1 class="text-3xl font-bold mb-6 text-gray-900 dark:text-white">Detail Pembayaran</h1>
          
          <!-- Success message -->
          <% if (order.payment_status === 'PAID') { %>
            <div class="mb-8 p-4 bg-green-50 border border-green-200 rounded-xl dark:bg-green-900/30 dark:border-green-800">
              <div class="flex items-start">
                <div class="flex-shrink-0">
                  <svg class="h-6 w-6 text-green-500 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                  </svg>
                </div>
                <div class="ml-3">
                  <h3 class="text-lg font-medium text-green-800 dark:text-green-200">Pembayaran Berhasil!</h3>
                  <div class="mt-2 text-green-700 dark:text-green-300">
                    <p>Subscription Anda telah aktif hingga <span class="font-semibold"><%= new Date(order.paid_at.setDate(order.paid_at.getDate() + subscription.duration_days)).toLocaleDateString('id-ID', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></span></p>
                  </div>
                </div>
              </div>
            </div>
          <% } %>
          
          <!-- Order details -->
          <div class="space-y-6">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
              <div>
                <p class="text-sm font-medium text-gray-500 dark:text-gray-400">Order ID</p>
                <p class="mt-1 text-lg font-mono font-semibold text-blue-700 dark:text-blue-400"><%= order.id %></p>
              </div>
              
              <div>
                <p class="text-sm font-medium text-gray-500 dark:text-gray-400">Tanggal</p>
                <p class="mt-1 text-gray-900 dark:text-gray-200"><%= new Date(order.created_at).toLocaleDateString('id-ID', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></p>
              </div>
              
              <div>
                <p class="text-sm font-medium text-gray-500 dark:text-gray-400">Status</p>
                <div class="mt-1">
                  <% if (order.payment_status === 'PAID') { %>
                    <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-green-100 text-green-800 dark:bg-green-900/50 dark:text-green-200 status-badge">
                      <svg class="-ml-1 mr-1.5 h-2 w-2 text-green-500 dark:text-green-400" fill="currentColor" viewBox="0 0 8 8">
                        <circle cx="4" cy="4" r="3" />
                      </svg>
                      LUNAS
                    </span>
                  <% } else if (order.payment_status === 'PENDING') { %>
                    <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-yellow-100 text-yellow-800 dark:bg-yellow-900/50 dark:text-yellow-200 status-badge">
                      <svg class="-ml-1 mr-1.5 h-2 w-2 text-yellow-500 dark:text-yellow-400" fill="currentColor" viewBox="0 0 8 8">
                        <circle cx="4" cy="4" r="3" />
                      </svg>
                      MENUNGGU PEMBAYARAN
                    </span>
                  <% } else { %>
                    <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-red-100 text-red-800 dark:bg-red-900/50 dark:text-red-200 status-badge">
                      <svg class="-ml-1 mr-1.5 h-2 w-2 text-red-500 dark:text-red-400" fill="currentColor" viewBox="0 0 8 8">
                        <circle cx="4" cy="4" r="3" />
                      </svg>
                      GAGAL
                    </span>
                  <% } %>
                </div>
              </div>
            </div>
            
            <div class="border-t border-gray-200 dark:border-gray-700 pt-6">
              <h2 class="text-lg font-semibold text-gray-900 dark:text-white">Detail Pelanggan</h2>
              <div class="mt-4 grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                  <p class="text-sm text-gray-500 dark:text-gray-400">Nama Lengkap</p>
                  <p class="mt-1 text-gray-900 dark:text-gray-200"><%= user.full_name %></p>
                </div>
                <div>
                  <p class="text-sm text-gray-500 dark:text-gray-400">Email</p>
                  <p class="mt-1 text-gray-900 dark:text-gray-200"><%= user.email %></p>
                </div>
              </div>
            </div>
            
            <div class="border-t border-gray-200 dark:border-gray-700 pt-6">
              <h2 class="text-lg font-semibold text-gray-900 dark:text-white">Detail Subscription</h2>
              <div class="mt-4">
                <div class="flex justify-between items-center p-4 bg-gray-50 rounded-lg dark:bg-gray-700/50">
                  <div>
                    <p class="font-medium text-gray-900 dark:text-white"><%= subscription.name %></p>
                    <p class="text-sm text-gray-500 dark:text-gray-400"><%= subscription.duration_days %> hari masa aktif</p>
                  </div>
                  <p class="text-lg font-bold text-green-700 dark:text-green-400">
                    Rp <%= new Intl.NumberFormat('id-ID').format(Number(subscription.price)) %>
                  </p>
                </div>
              </div>
            </div>
          </div>
          
          <!-- Payment button -->
          <div class="mt-10 border-t border-gray-200 dark:border-gray-700 pt-6">
            <% if (order.payment_status === 'PENDING') { %>
              <button id="pay-button" class="w-full md:w-auto px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-lg shadow-sm transition-colors duration-150 flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
                  <path d="M4 4a2 2 0 00-2 2v1h16V6a2 2 0 00-2-2H4z" />
                  <path fill-rule="evenodd" d="M18 9H2v5a2 2 0 002 2h12a2 2 0 002-2V9zM4 13a1 1 0 011-1h1a1 1 0 110 2H5a1 1 0 01-1-1zm5-1a1 1 0 100 2h1a1 1 0 100-2H9z" clip-rule="evenodd" />
                </svg>
                Lanjut Pembayaran
              </button>
            <% } else if (order.payment_status === 'PAID') { %>
              <div class="flex flex-col sm:flex-row gap-4">
                <a href="/dashboard" class="px-6 py-3 bg-green-600 hover:bg-green-700 text-white font-medium rounded-lg shadow-sm transition-colors duration-150 text-center">
                  Kembali ke Dashboard
                </a>
                <a href="#" class="px-6 py-3 bg-white border border-gray-300 hover:bg-gray-50 text-gray-700 font-medium rounded-lg shadow-sm transition-colors duration-150 text-center dark:bg-gray-700 dark:border-gray-600 dark:text-white dark:hover:bg-gray-600">
                  Lihat Subscription
                </a>
              </div>
            <% } %>
          </div>
        </div>
      </div>
      
      <!-- Payment information sidebar -->
      <div class="w-full lg:w-1/3">
        <div class="payment-card bg-white rounded-xl p-6 dark:bg-gray-800 sticky top-6">
          <h2 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">Instruksi Pembayaran</h2>
          
          <% if (order.payment_status === 'PENDING') { %>
            <div class="space-y-4">
              <div class="p-4 bg-blue-50 rounded-lg dark:bg-blue-900/20">
                <h3 class="font-medium text-blue-800 dark:text-blue-200">Virtual Account</h3>
                <p class="mt-1 text-sm text-blue-700 dark:text-blue-300">Gunakan Virtual Account untuk transfer dari bank manapun</p>
              </div>
              
              <div class="p-4 bg-purple-50 rounded-lg dark:bg-purple-900/20">
                <h3 class="font-medium text-purple-800 dark:text-purple-200">E-Wallet</h3>
                <p class="mt-1 text-sm text-purple-700 dark:text-purple-300">Bayar menggunakan GoPay, OVO, atau Dana</p>
              </div>
              
              <div class="p-4 bg-green-50 rounded-lg dark:bg-green-900/20">
                <h3 class="font-medium text-green-800 dark:text-green-200">Kartu Kredit</h3>
                <p class="mt-1 text-sm text-green-700 dark:text-green-300">Visa, Mastercard, atau JCB</p>
              </div>
              
              <div class="p-4 bg-gray-50 rounded-lg dark:bg-gray-700">
                <h3 class="font-medium text-gray-800 dark:text-gray-200">Konvenien Store</h3>
                <p class="mt-1 text-sm text-gray-700 dark:text-gray-300">Bayar di Alfamart, Indomaret, atau merchant lainnya</p>
              </div>
            </div>
          <% } else if (order.payment_status === 'PAID') { %>
            <div class="p-4 bg-green-50 rounded-lg dark:bg-green-900/20">
              <h3 class="font-medium text-green-800 dark:text-green-200">Pembayaran Berhasil</h3>
              <p class="mt-2 text-sm text-green-700 dark:text-green-300">Terima kasih telah melakukan pembayaran. Subscription Anda sekarang aktif.</p>
              <div class="mt-3 p-3 bg-white rounded dark:bg-gray-700">
                <p class="text-xs font-mono text-gray-500 dark:text-gray-400">ID Transaksi: <%= order.id %></p>
                <p class="text-xs font-mono text-gray-500 dark:text-gray-400 mt-1"><%= new Date(order.paid_at).toLocaleString('id-ID') %></p>
              </div>
            </div>
          <% } %>
        </div>
      </div>
    </div>
  </div>

  <script>
    document.getElementById('pay-button')?.addEventListener('click', function() {
      const button = this;
      button.disabled = true;
      button.innerHTML = `
        <svg class="animate-spin -ml-1 mr-2 h-5 w-5 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
          <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
          <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
        </svg>
        Memproses...
      `;
      
      fetch(`/api/order/<%= order.id %>/pay`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        }
      })
      .then(response => response.json())
      .then(data => {
        snap.pay(data.snap_token, {
          onSuccess: function(result) {
            window.location.reload();
          },
          onPending: function(result) {
            window.location.reload();
          },
          onError: function(result) {
            window.location.reload();
          },
          onClose: function() {
            button.disabled = false;
            button.innerHTML = `
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
                <path d="M4 4a2 2 0 00-2 2v1h16V6a2 2 0 00-2-2H4z" />
                <path fill-rule="evenodd" d="M18 9H2v5a2 2 0 002 2h12a2 2 0 002-2V9zM4 13a1 1 0 011-1h1a1 1 0 110 2H5a1 1 0 01-1-1zm5-1a1 1 0 100 2h1a1 1 0 100-2H9z" clip-rule="evenodd" />
              </svg>
              Lanjut Pembayaran
            `;
          }
        });
      })
      .catch(error => {
        console.error('Error:', error);
        button.disabled = false;
        button.innerHTML = `
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
            <path d="M4 4a2 2 0 00-2 2v1h16V6a2 2 0 00-2-2H4z" />
            <path fill-rule="evenodd" d="M18 9H2v5a2 2 0 002 2h12a2 2 0 002-2V9zM4 13a1 1 0 011-1h1a1 1 0 110 2H5a1 1 0 01-1-1zm5-1a1 1 0 100 2h1a1 1 0 100-2H9z" clip-rule="evenodd" />
          </svg>
          Lanjut Pembayaran
        `;
        alert('Gagal memulai pembayaran. Silakan coba lagi.');
      });
    });
  </script>
</body>
</html>